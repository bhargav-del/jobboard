{
  "name": "jobboard",
  "version": "1.0.5",
  "description": "Self-contained Windows desktop job application pipeline for roles and follow-ups.",
  "private": true,
  "main": "desktop/main.cjs",
  "scripts": {
    "dist": "electron-builder --win portable",
    "check": "node --check app.js"
  },
  "devDependencies": {
    "electron": "^36.3.2",
    "electron-builder": "^26.0.12",
    "@capacitor/android": "^7.4.0",
    "@capacitor/cli": "^7.4.0",
    "@capacitor/core": "^8.5.2"
  },
  "build": {
    "appId": "dev.yuin.jobboard",
    "productName": "Jobboard",
    "directories": {
      "output": "release"
    },
    "files": [
      "index.html",
      "styles.css",
      "app.js",
      "desktop/**/*",
      "package.json"
    ],
    "win": {
      "target": [
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        },
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ]
    },
    "asar": true,
    "portable": {
      "artifactName": "Jobboard-Portable-${version}.exe"
    },
    "nsis": {
      "artifactName": "Jobboard-Setup-${version}.exe",
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    }
  }
}
